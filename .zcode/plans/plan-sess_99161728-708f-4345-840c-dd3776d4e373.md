Apply the portfolio content audit. Three files change; no layout/system files are touched. No commit, no push — verify locally with dev server + browser.

## 1. `index.html`

**SEO (lines 8–9):** Title → `Harikrishnan P V — Lead Product & Platform Engineer | AI-Native Systems`; meta description → "…architecting AI-native full-stack platforms from 0 to production with Spec-Driven Development (B-MAD), Next.js, AWS, and GCP."

**Hero (line 77):** Replace paragraph with the audit copy: "I architect and ship full-stack platforms from 0 to production — pairing rigorous Spec-Driven Development (B-MAD) and agentic workflows with distributed cloud, modern web, and physical sensor systems."

**Work / bento (lines 111–186) — 7 cards kept:**
- NEW featured card first: **OfficeKit HRMS Platform Revamp** (`Enterprise SaaS` badge, `bi-diagram-3` icon, opens modal) — audit copy: GCP modernization, decoupled Spring Boot microservices, automated shift scheduling engine, multimodal AI LMS. Footer: `Java Spring Boot • PostgreSQL • React • GCP • Claude Code / Cursor`.
- Update **Brixline** card: title `Brixline Listing & Search Engine`, badge `Cloud Architecture`, audit description, footer `Next.js • AWS Lambda • CloudFront • TypeScript • PostgreSQL`.
- Update **Spectrometer** card: title `Spectrometer Field Telemetry Interface`, badge `IoT / Hardware`, audit copy (SSH/SFTP acquisition, 80% reduction), footer `Flutter • Python • Embedded Linux • Firebase`.
- Update **Reflex** card: badge `0-to-1 SaaS`, audit copy (multi-tenant ERP/POS, 196+ Edge Functions, RLS, 35% latency reduction), footer `Next.js (App Router) • TypeScript • Supabase / PostgreSQL • Edge Functions • Flutter`.
- AI Property, Construction MVP, Optical E-commerce cards unchanged.
- DOM order: OfficeKit(F), Spectrometer(c), Brixline(F), AI Property(c), Reflex(F), Construction(c), E-commerce(c) — tiles cleanly at lg (3-col: featured+compact per row, lone compact last row) and md/mobile.

**Stack section (lines 200–276):** Replace the two marquee rows with **5 categorized cluster cards** (user-approved layout change), reusing the existing `.card` + `.skill-tag` styles to keep the design language: Core & Architecture (bi-diagram-3), Frontend & Mobile (bi-window-sidebar), Backend, Cloud & Database (bi-hdd-network), AI & Engineering Productivity (bi-robot), Hardware & Optics (bi-eye). Tags exactly per the audit's clusters (35 tags). Grid: 2-col on md+, first card spans 2 columns; keep `<Stack/>` heading and data-reveal animations. Marquee CSS left in place (unused, harmless); duplicate-set issue disappears with the marquee.

**Journey (lines 286–393):**
- M2H: keep header/date; replace single paragraph with 3 audit bullets (modernization leadership, B-MAD/Spec-Driven with Cursor & Claude Code + 40% faster sprints, OfficeKit modules on GCP) as a styled `<ul>` matching existing text classes.
- Brixline: date → `Jun 2025 – Nov 2025 · Bangalore`; 2 audit bullets (Next.js + AWS serverless; URL-state sync, debounced filters, sub-100ms edge-cached previews).
- Reflex: drop `(Part-Time)`, date → `Sep 2022 – Jan 2026 · Bangalore`; 3 audit bullets (0→production multi-tenant ERP/POS; 196+ Edge Functions, RLS, atomic inventory ops; Flutter store-floor app). Move entry before Infiniti per audit order.
- Infiniti: 2 audit bullets (Flutter instrumentation, 80% reduction; cloud-backed chemometric dashboards).
- Renumber the mislabeled HTML comments; reassign data-delay stagger.

## 2. `src/modules/typewriter.js`
Line 6 → `'Lead Product & Platform Engineer | AI-Native Systems'`.

## 3. `src/modules/modal.js`
- Add `officekit` entry to `projectsData` (title, tags, audit description, HTML detail sections: microservices architecture, real-time shift scheduling, multimodal AI LMS, B-MAD workflow; `images: []`).
- Make the modal carousel block conditional (`project.images.length > 0`) so OfficeKit renders detail without an empty image frame; carousel listeners already null-safe.
- Fix bug: `images/brixline/dashboard.png` doesn't exist → use `list.png`.
- Update `reflex` modal tags/description to audit copy (RLS, 35% latency reduction, Flutter).

## Verification (no commit / no push)
1. `npm run build` — sanity check.
2. `npm run dev` in background.
3. Browser GUI test: hero typewriter + new copy; 7-card bento alignment (desktop + mobile widths); OfficeKit/Brixline/Reflex modals; stack category cards; journey dates/bullets/order; dark + light theme spot-check. Fix any wrapping/overflow issues found (trim long strings only if they break).